# Central version configuration for the base image.
# This file is sourced by the Makefile targets (and can be sourced in CI).

# Image identity
IMAGE_NAME=agentman-base
IMAGE_TAG=dev

# Comma-separated list for buildx (e.g. "linux/amd64,linux/arm64")
PLATFORMS=linux/amd64

# Base image
DEBIAN_TAG=trixie-slim

# Tool versions
RUSTUP_VERSION=1.27.1
RUST_TOOLCHAIN=1.92.0

GO_VERSION=1.25.5
BUN_VERSION=1.3.5
UV_VERSION=0.9.22
PYTHON_VERSION=3.13

# DuckDB CLI
DUCKDB_VERSION=1.4.3

# opencode CLI
OPENCODE_VERSION=v1.1.7

# Container user
USERNAME=agent
USER_UID=1000
USER_GID=1000

